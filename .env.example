# Algolia — scraper + backend enrichment scripts
# Admin-scoped key (addObject, deleteObject, editSettings, ...), restricted to archive_* indices
ALGOLIA_APP_ID=
ALGOLIA_API_KEY=

# Frontend — search-only key (generated from ALGOLIA_API_KEY at build time, or provided separately)
# Added during Phase 3
# ALGOLIA_SEARCH_API_KEY=

# Phase 4 — artist metadata enrichment
# DISCOGS_TOKEN=
# LASTFM_API_KEY=

# Phase 5 — LLM mood tagging via Algolia-internal gateway
# LLM_ENABLERS_API_URL=
# LLM_ENABLERS_API_TOKEN=
